train:
  profile: smoke
  max_epochs: 2
  steps_per_epoch: 2
  val_steps: 1
  batch_cells: 32
  learning_rate: 0.001
  weight_decay: 0.0001
  grad_clip_norm: 1.0
  mixed_precision: false
  device: cpu
  seed: 42
