defaults:
  - model: stagebridge
  - training: default
  - data: default
  - splits: donor_holdout
  - experiment: full_benchmark
  - _self_

run_name: stagebridge_full_benchmark
output_dir: outputs
seed: 42

hydra:
  job:
    chdir: false
