\documentclass[border=5pt, tikz]{standalone}

\usetikzlibrary{
    positioning, arrows.meta, calc, backgrounds,
    decorations.markings, decorations.pathmorphing,
    fit, shapes.geometric, patterns, matrix,
    shadows.blur, fadings
}

\usepackage{amsmath, amssymb, bm}
\usepackage{xcolor}

% ============================================
% MODERN PALETTE
% ============================================
\definecolor{primary}{HTML}{2563EB}
\definecolor{secondary}{HTML}{7C3AED}
\definecolor{accent}{HTML}{F59E0B}
\definecolor{success}{HTML}{10B981}
\definecolor{danger}{HTML}{EF4444}
\definecolor{neutral}{HTML}{6B7280}

\definecolor{bg}{HTML}{FAFBFC}
\definecolor{bgdark}{HTML}{F1F5F9}
\definecolor{border}{HTML}{E2E8F0}
\definecolor{text}{HTML}{1E293B}
\definecolor{textsub}{HTML}{64748B}

\definecolor{receiver}{HTML}{DC2626}
\definecolor{neighbor}{HTML}{0891B2}
\definecolor{hlca}{HTML}{059669}
\definecolor{luca}{HTML}{7C3AED}
\definecolor{context}{HTML}{2563EB}
\definecolor{drift}{HTML}{F59E0B}
\definecolor{flow}{HTML}{EC4899}

% ============================================
% STYLES
% ============================================
\tikzset{
    cell/.style={circle, minimum size=4mm, fill=#1!70, draw=#1!90, line width=0.5pt,
        blur shadow={shadow blur steps=5, shadow xshift=0.3mm, shadow yshift=-0.3mm}},
    cellsm/.style={circle, minimum size=3mm, fill=#1!60, draw=#1!80, line width=0.4pt},
    rcell/.style={circle, minimum size=6mm, fill=receiver!80, draw=receiver!90, line width=0.8pt,
        blur shadow={shadow blur steps=8, shadow xshift=0.5mm, shadow yshift=-0.5mm}},
    module/.style={rectangle, rounded corners=3pt, minimum height=8mm, minimum width=22mm,
        fill=#1!15, draw=#1!60, line width=0.6pt, font=\fontsize{8}{9}\selectfont\sffamily,
        blur shadow={shadow blur steps=5, shadow xshift=0.3mm, shadow yshift=-0.3mm}},
    modulelg/.style={rectangle, rounded corners=4pt, minimum height=10mm, minimum width=30mm,
        fill=#1!12, draw=#1!50, line width=0.7pt, font=\fontsize{9}{10}\selectfont\sffamily\bfseries,
        blur shadow={shadow blur steps=6, shadow xshift=0.4mm, shadow yshift=-0.4mm}},
    panel/.style={rectangle, rounded corners=6pt, fill=white, draw=border, line width=0.6pt,
        blur shadow={shadow blur steps=8, shadow xshift=0.5mm, shadow yshift=-0.5mm, shadow opacity=0.15}},
    subpanel/.style={rectangle, rounded corners=4pt, fill=bgdark, draw=border!70, line width=0.4pt},
    flow/.style={-{Stealth[length=2.5mm, width=1.8mm]}, line width=0.7pt, draw=#1},
    flowthick/.style={-{Stealth[length=3mm, width=2mm]}, line width=1pt, draw=#1},
    flowdashed/.style={-{Stealth[length=2.5mm, width=1.8mm]}, line width=0.7pt, draw=#1, dashed},
    heading/.style={font=\fontsize{11}{12}\selectfont\bfseries\sffamily, text=text},
    subheading/.style={font=\fontsize{9}{10}\selectfont\sffamily, text=textsub},
    label/.style={font=\fontsize{7}{8}\selectfont\sffamily, text=textsub},
    mathlabel/.style={font=\fontsize{8}{9}\selectfont, text=text},
    dim/.style={rectangle, rounded corners=2pt, fill=#1!10, draw=#1!40, line width=0.3pt,
        font=\fontsize{7}{8}\selectfont\sffamily, inner sep=2pt},
    eqbox/.style={rectangle, rounded corners=3pt, fill=bgdark, draw=border, line width=0.4pt,
        inner sep=4pt, font=\fontsize{8}{9}\selectfont},
}

\begin{document}

% ============================================
% PAGE 1: ARCHITECTURE OVERVIEW
% ============================================
\begin{tikzpicture}[x=1mm, y=1mm]

\fill[bg] (-5, -5) rectangle (280, 165);

% Header
\node[font=\fontsize{14}{15}\selectfont\bfseries\sffamily, text=text, anchor=west] at (0, 160) {StageBridge v1.2};
\node[font=\fontsize{9}{10}\selectfont\sffamily, text=textsub, anchor=west] at (55, 160) {Architecture Overview};
\draw[primary, line width=2pt] (0, 154) -- (275, 154);

% ============================================
% TIER 1: REFERENCE GEOMETRY (Left)
% ============================================
\node[panel, minimum width=85mm, minimum height=70mm] at (42.5, 115) {};
\node[heading, anchor=west] at (3, 147) {1. Reference Geometry};

% HLCA manifold
\begin{scope}[shift={(10, 115)}]
    \node[subpanel, minimum width=30mm, minimum height=28mm] at (0, 8) {};
    \node[subheading] at (0, 20) {HLCA (Healthy)};

    \draw[hlca!40, line width=1.2pt, fill=hlca!8]
        plot[smooth cycle, tension=0.6] coordinates {(-10,12) (-4,16) (4,14) (10,10) (8,2) (2,-2) (-6,0) (-12,6)};

    \foreach \x/\y/\s in {-6/12/1.2, 0/13/1.4, 6/10/1.2, -4/6/1, 4/4/1, -2/0/0.8} {
        \fill[hlca] (\x, \y) circle (\s mm);
    }
    \node[dim=hlca] at (0, -6) {$z_H \in \mathbb{R}^{30}$};
\end{scope}

% LuCA manifold
\begin{scope}[shift={(55, 115)}]
    \node[subpanel, minimum width=30mm, minimum height=28mm] at (0, 8) {};
    \node[subheading] at (0, 20) {LuCA (Cancer)};

    \draw[luca!40, line width=1.2pt, fill=luca!8]
        plot[smooth cycle, tension=0.6] coordinates {(-10,10) (-4,16) (6,14) (12,8) (6,0) (0,-2) (-8,2) (-12,6)};

    \foreach \x/\y/\s in {-4/12/1.2, 2/13/1.4, 8/9/1.2, -2/6/1, 4/3/1, 0/-1/0.8} {
        \fill[luca] (\x, \y) circle (\s mm);
    }
    \node[dim=luca] at (0, -6) {$z_L \in \mathbb{R}^{10}$};
\end{scope}

% Learned GW Fusion
\begin{scope}[shift={(42.5, 82)}]
    \node[modulelg=accent, minimum width=70mm] at (0, 0) {Learned GW Fusion};
    \node[eqbox, anchor=north] at (0, -8) {%
        \begin{minipage}{65mm}\centering
        $\pi_H, \pi_L$: learned projections to metric space\\[2pt]
        $\mathcal{L}_{\mathrm{struct}} = \|D_H - D_L\|_F^2$ (training only)\\[2pt]
        Output: $[\alpha\pi_H(z_H) + (1{-}\alpha)\pi_L(z_L);\; \pi_H - \pi_L]$
        \end{minipage}
    };

    \draw[flow=hlca] (-20, 15) -- (-20, 5);
    \draw[flow=luca] (20, 15) -- (20, 5);
\end{scope}

% ============================================
% TIER 2: NICHE ENCODING (Middle)
% ============================================
\node[panel, minimum width=85mm, minimum height=70mm] at (137.5, 115) {};
\node[heading, anchor=west] at (98, 147) {2. Niche Encoding (AMICI)};

% Spatial visualization
\begin{scope}[shift={(115, 122)}]
    \fill[neighbor!6] (0,0) circle (18mm);
    \fill[neighbor!10] (0,0) circle (12mm);
    \fill[neighbor!15] (0,0) circle (6mm);

    \foreach \a/\r/\op in {30/14/40, 70/16/35, 120/13/45, 170/15/35, 220/14/40, 280/12/50, 330/16/35} {
        \node[cellsm=neighbor, opacity=\op/100] at (\a:\r mm) {};
    }

    \node[rcell] at (0, 0) {};
    \node[font=\fontsize{5}{6}\selectfont, text=white] at (0, 0) {recv};

    \node[label] at (0, -22) {Spatial neighbors by distance};
\end{scope}

% Encoder stack
\begin{scope}[shift={(157, 115)}]
    \node[module=neighbor, minimum width=26mm] (proj) at (0, 18) {Input proj};
    \node[modulelg=context, minimum width=26mm] (attn) at (0, 6) {Dist-Attn};
    \node[module=context, minimum width=26mm] (ffn) at (0, -6) {FFN};
    \node[module=receiver, minimum width=26mm] (ssl) at (0, -18) {SSL: $\hat{x}_i$};

    \draw[flow=neighbor] (proj) -- (attn);
    \draw[flow=context] (attn) -- (ffn);
    \draw[flowdashed=receiver] (ffn) -- (ssl);

    \node[eqbox, anchor=west] at (16, 6) {$\alpha_j \propto e^{-d_j/\sigma}$};
\end{scope}

% Token assembly
\begin{scope}[shift={(137.5, 82)}]
    \node[subheading] at (0, 10) {Context Refiner (Set Transformer)};

    % Token circles
    \fill[context!60] (-28, 0) circle (3mm);
    \fill[hlca!50] (-18, 0) circle (2.5mm);
    \fill[luca!50] (-10, 0) circle (2.5mm);
    \fill[accent!50] (-2, 0) circle (2.5mm);

    \node[label] at (-28, -6) {niche};
    \node[label] at (-14, -6) {refs};
    \node[label] at (-2, -6) {path};

    \node[modulelg=context, minimum width=35mm] at (22, 0) {ISAB$\times$2 + SAB + PMA};

    \draw[flow=context] (-28, 0) -- (2, 0);
\end{scope}

% ============================================
% TIER 3: TRANSITION DYNAMICS (Right)
% ============================================
\node[panel, minimum width=85mm, minimum height=70mm] at (232.5, 115) {};
\node[heading, anchor=west] at (193, 147) {3. Transition Dynamics};

% CrossAttentionDrift
\begin{scope}[shift={(210, 122)}]
    \node[subpanel, minimum width=40mm, minimum height=32mm] at (0, 0) {};
    \node[subheading] at (0, 14) {CrossAttentionDrift};

    \node[module=flow, minimum width=12mm, minimum height=6mm] (xt) at (-12, 4) {$x_t$};
    \node[module=secondary, minimum width=12mm, minimum height=6mm] (tau) at (-12, -4) {$\tau$};

    \node[module=context, minimum width=16mm] (attn) at (4, 0) {Attn};
    \node[module=drift, minimum width=10mm] (gate) at (16, 0) {$g$};

    \draw[flow=flow] (xt) -- (attn);
    \draw[flow=secondary] (tau) -- (attn);
    \draw[flow=context] (attn) -- (gate);

    \node[label] at (0, -18) {$v_\theta = g \cdot v_c + (1{-}g) \cdot v_\ell$};
\end{scope}

% OT-CFM
\begin{scope}[shift={(255, 122)}]
    \node[subpanel, minimum width=35mm, minimum height=32mm] at (0, 0) {};
    \node[subheading] at (0, 14) {OT-CFM};

    % Source/target
    \fill[success!30, rounded corners=2pt] (-12, -8) rectangle (-4, 8);
    \fill[danger!30, rounded corners=2pt] (4, -8) rectangle (12, 8);

    \foreach \y in {-4, 0, 4} {
        \fill[success] (-8, \y) circle (1mm);
        \fill[danger] (8, \y) circle (1mm);
    }

    \draw[flow, line width=0.8pt] (-4, 0) -- (4, 0);

    \node[label] at (-8, -12) {$\rho_0$};
    \node[label] at (8, -12) {$\rho_1$};
\end{scope}

% Stats conditioning
\begin{scope}[shift={(232.5, 82)}]
    \node[modulelg=accent, minimum width=70mm] at (0, 0) {FiLM: Stats Conditioning};
    \node[eqbox, anchor=north] at (0, -8) {%
        \begin{minipage}{60mm}\centering
        Cell cycle, clonal, CAF features\\
        Applied AFTER context refinement
        \end{minipage}
    };
\end{scope}

% ============================================
% FLOW ARROWS BETWEEN TIERS
% ============================================
\draw[flowthick=accent] (85, 82) -- (95, 82);
\draw[flowthick=context] (180, 82) -- (190, 82);

% ============================================
% BOTTOM: LOSS FUNCTIONS
% ============================================
\node[panel, minimum width=270mm, minimum height=38mm] at (137.5, 35) {};
\node[heading, anchor=west] at (5, 52) {Training Objectives};

\begin{scope}[shift={(5, 30)}]
    % SSL
    \node[modulelg=receiver, minimum width=55mm] at (30, 0) {SSL Reconstruction};
    \node[eqbox, anchor=north] at (30, -8) {$\mathcal{L}_{\mathrm{ssl}} = \|x_i - \hat{x}_i\|^2$};
    \node[label, anchor=north] at (30, -20) {Predict receiver from neighbors only};

    % OT-CFM
    \node[modulelg=drift, minimum width=55mm] at (100, 0) {Flow Matching};
    \node[eqbox, anchor=north] at (100, -8) {$\mathcal{L}_{\mathrm{cfm}} = \|v_\theta(x_t, t | \mathbf{c}) - (x_1 - x_0)\|^2$};
    \node[label, anchor=north] at (100, -20) {OT-coupled pairs, Sinkhorn};

    % GW Structure
    \node[modulelg=accent, minimum width=55mm] at (170, 0) {GW Structure};
    \node[eqbox, anchor=north] at (170, -8) {$\mathcal{L}_{\mathrm{gw}} = \|D_H - D_L\|_F^2$};
    \node[label, anchor=north] at (170, -20) {Distance matrices match};

    % Auxiliary
    \node[modulelg=secondary, minimum width=55mm] at (240, 0) {Auxiliary};
    \node[eqbox, anchor=north] at (240, -8) {Pathway + Proliferation};
    \node[label, anchor=north] at (240, -20) {PROGENy, Ki67};
\end{scope}

\end{tikzpicture}

% ============================================
% PAGE 2: EQUATIONS
% ============================================
\newpage
\begin{tikzpicture}[x=1mm, y=1mm]

\fill[bg] (-5, -5) rectangle (280, 165);

% Header
\node[font=\fontsize{14}{15}\selectfont\bfseries\sffamily, text=text, anchor=west] at (0, 160) {StageBridge v1.2};
\node[font=\fontsize{9}{10}\selectfont\sffamily, text=textsub, anchor=west] at (55, 160) {Mathematical Formulation};
\draw[primary, line width=2pt] (0, 154) -- (275, 154);

% ============================================
% LEFT COLUMN: CORE EQUATIONS
% ============================================
\node[panel, minimum width=130mm, minimum height=145mm] at (67.5, 78) {};
\node[heading, anchor=west] at (5, 147) {Core Equations};

% 1. Distance-Weighted Attention
\begin{scope}[shift={(10, 130)}]
    \node[subheading, text=context, anchor=west] at (0, 0) {1. AMICI Distance-Weighted Attention};
    \node[anchor=west] at (5, -12) {%
        $\displaystyle\alpha_{ij} = \frac{\exp(-d_{ij}/\sigma_h) \cdot \exp(q_i^\top k_j / \sqrt{d})}%
        {\sum_{k \in \mathcal{N}_i} \exp(-d_{ik}/\sigma_h) \cdot \exp(q_i^\top k_k / \sqrt{d})}$
    };
    \node[label, anchor=west] at (5, -24) {Each head $h$ learns its own distance scale $\sigma_h$};
\end{scope}

% 2. SSL Reconstruction
\begin{scope}[shift={(10, 95)}]
    \node[subheading, text=receiver, anchor=west] at (0, 0) {2. SSL Receiver Reconstruction};
    \node[anchor=west] at (5, -12) {%
        $\hat{x}_i = f_\theta\Bigl(\sum_{j \in \mathcal{N}_i} \alpha_{ij} \cdot v_j\Bigr)$, \quad
        $\mathcal{L}_{\mathrm{ssl}} = \|x_i - \hat{x}_i\|_2^2$
    };
    \node[label, anchor=west] at (5, -24) {Predict receiver from neighbor context only (no residuals)};
\end{scope}

% 3. Learned GW Fusion
\begin{scope}[shift={(10, 60)}]
    \node[subheading, text=accent, anchor=west] at (0, 0) {3. Learned GW Fusion (Per-Cell)};
    \node[anchor=west] at (5, -12) {%
        $h = \pi_H(z_H)$, \quad $l = \pi_L(z_L)$, \quad $z_f = [\alpha h + (1{-}\alpha)l;\; h - l]$
    };
    \node[anchor=west] at (5, -24) {%
        $\mathcal{L}_{\mathrm{struct}} = \|D_H - D_L\|_F^2$ where $D_H[i,j] = \|h_i - h_j\|$
    };
    \node[label, anchor=west] at (5, -36) {Structure loss guides training; inference is per-cell};
\end{scope}

% 4. Context Refinement
\begin{scope}[shift={(10, 22)}]
    \node[subheading, text=context, anchor=west] at (0, 0) {4. Hierarchical Set Transformer};
    \node[anchor=west] at (5, -12) {%
        $\mathbf{c} = \mathrm{PMA}(\mathrm{SAB}(\mathrm{ISAB}(\mathrm{ISAB}([e_{\mathrm{niche}}, z_H, z_L, p]))))$
    };
    \node[anchor=west] at (5, -24) {%
        FiLM: $\mathbf{c}' = \gamma(s) \odot \mathbf{c} + \beta(s)$ where $s$ = stats token
    };
    \node[label, anchor=west] at (5, -36) {Stats conditioning AFTER attention (prevents shortcut)};
\end{scope}

% ============================================
% RIGHT COLUMN: DYNAMICS & SYMBOLS
% ============================================
\node[panel, minimum width=130mm, minimum height=72mm] at (207.5, 114) {};
\node[heading, anchor=west] at (145, 147) {Transition Dynamics};

% CrossAttentionDrift
\begin{scope}[shift={(150, 130)}]
    \node[subheading, text=drift, anchor=west] at (0, 0) {CrossAttentionDrift (Gated)};
    \node[anchor=west] at (5, -12) {%
        $v_c = \mathrm{CrossAttn}(Q{=}[x_t, \tau], KV{=}\mathbf{c})$
    };
    \node[anchor=west] at (5, -24) {%
        $v_\ell = \mathrm{MLP}([x_t, \tau])$
    };
    \node[anchor=west] at (5, -36) {%
        $v_\theta = g \odot v_c + (1{-}g) \odot v_\ell$, \quad $g = \sigma(W_g[\cdot])$
    };
\end{scope}

% OT-CFM
\begin{scope}[shift={(150, 90)}]
    \node[subheading, text=flow, anchor=west] at (0, 0) {OT-CFM Loss};
    \node[anchor=west] at (5, -12) {%
        $(x_0, x_1) \sim \pi^*_{\mathrm{Sinkhorn}}$, \quad $x_t = (1{-}t)x_0 + tx_1$
    };
    \node[anchor=west] at (5, -24) {%
        $\mathcal{L}_{\mathrm{cfm}} = \mathbb{E}_{t \sim U(0,1)}\bigl[\|v_\theta(x_t, t | \mathbf{c}) - (x_1 - x_0)\|^2\bigr]$
    };
\end{scope}

% Symbol Reference
\node[panel, minimum width=130mm, minimum height=68mm] at (207.5, 42) {};
\node[heading, anchor=west] at (145, 73) {Symbol Reference};

\begin{scope}[shift={(150, 60)}]
    \node[anchor=west, font=\fontsize{8}{9}\selectfont\sffamily] at (0, 0) {%
        \begin{tabular}{lll}
        \textbf{Symbol} & \textbf{Dim} & \textbf{Description} \\[2pt]
        $x_i$ & $\mathbb{R}^{40}$ & Cell embedding (HLCA 30 + LuCA 10) \\
        $d_{ij}$ & $\mathbb{R}^+$ & Spatial distance (microns) \\
        $\mathcal{N}_i$ & set & $K$-nearest neighbors of cell $i$ \\
        $z_H, z_L$ & 30, 10 & HLCA/LuCA reference latents \\
        $p$ & $\mathbb{R}^{14}$ & PROGENy pathway activities \\
        $s$ & $\mathbb{R}^{5}$ & Stats token (cycle, clonal, CAF) \\
        $\mathbf{c}$ & $\mathbb{R}^{256}$ & Niche context embedding \\
        $v_\theta$ & $\mathbb{R}^{40}$ & Predicted velocity \\
        $\tau$ & $\mathbb{R}^{32}$ & Sinusoidal time embedding \\
        \end{tabular}
    };
\end{scope}

\end{tikzpicture}

\end{document}
