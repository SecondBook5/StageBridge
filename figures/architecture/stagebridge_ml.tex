\documentclass[border=10pt, tikz]{standalone}

\usetikzlibrary{
    positioning,
    arrows.meta,
    calc,
    backgrounds,
    decorations.markings,
    fit,
    shapes.geometric
}

\usepackage{amsmath, amssymb}
\usepackage{xcolor}

% ============================================
% PALETTE
% ============================================
\definecolor{accent}{HTML}{E07B54}
\definecolor{encoder}{HTML}{5DADE2}
\definecolor{encoderdark}{HTML}{2E86C1}
\definecolor{attention}{HTML}{AF7AC5}
\definecolor{attentiondark}{HTML}{884EA0}
\definecolor{drift}{HTML}{58D68D}
\definecolor{driftdark}{HTML}{28B463}
\definecolor{gate}{HTML}{F4D03F}
\definecolor{gatedark}{HTML}{D4AC0D}

\definecolor{recv}{HTML}{E74C3C}
\definecolor{spatial}{HTML}{5DADE2}
\definecolor{refcol}{HTML}{58D68D}
\definecolor{auxcol}{HTML}{9B59B6}

\definecolor{blockbg}{HTML}{F5F7FA}
\definecolor{blockline}{HTML}{B0B8C0}
\definecolor{textmain}{HTML}{1A1A2E}
\definecolor{textsub}{HTML}{5A6070}

% ============================================
% STYLES
% ============================================
\tikzset{
    % Shadowed blocks for multi-head
    shadowed/.style={
        preaction={fill=#1!12, transform canvas={xshift=1.8pt, yshift=-1.8pt}},
        preaction={fill=#1!22, transform canvas={xshift=0.9pt, yshift=-0.9pt}},
        fill=#1!32
    },
    % Layer blocks
    layer/.style={
        rectangle, rounded corners=2mm,
        minimum height=2em, minimum width=2.8cm,
        draw=#1!70, line width=0.5pt, fill=#1!18,
        font=\fontsize{7}{8}\selectfont\sffamily, text=textmain
    },
    layersmall/.style={
        rectangle, rounded corners=1.5mm,
        minimum height=1.6em, minimum width=1.6cm,
        draw=#1!70, line width=0.4pt, fill=#1!18,
        font=\fontsize{6}{7}\selectfont\sffamily, text=textmain
    },
    % Multi-head block
    mhblock/.style={
        rectangle, rounded corners=2mm,
        minimum height=2.2em, minimum width=3cm,
        draw=#1!70, line width=0.5pt, shadowed=#1,
        font=\fontsize{7}{8}\selectfont\sffamily, text=textmain
    },
    % Background block
    bgblock/.style={
        rectangle, rounded corners=4mm,
        fill=blockbg, draw=blockline, line width=0.4pt
    },
    % Circles
    iocircle/.style={
        circle, minimum size=2.4em,
        draw=#1!70, fill=#1!22, line width=0.5pt,
        font=\fontsize{7}{8}\selectfont\sffamily
    },
    % Tokens
    tok/.style={
        rectangle, rounded corners=1pt,
        fill=#1!40, draw=#1!60,
        minimum height=5.5mm, minimum width=5.5mm, line width=0.35pt
    },
    toklabel/.style={
        font=\fontsize{4}{5}\selectfont\sffamily, text=textmain
    },
    % Matrix
    matrixbox/.style={
        rectangle, rounded corners=1mm,
        fill=#1!12, draw=#1!45, line width=0.4pt
    },
    % Panel
    panel/.style={
        rectangle, rounded corners=5pt,
        fill=white, draw=blockline, line width=0.5pt
    },
    % Text styles
    title/.style={font=\fontsize{10}{11}\selectfont\bfseries\sffamily, text=textmain},
    subtitle/.style={font=\fontsize{7}{8}\selectfont\sffamily, text=textsub},
    annot/.style={font=\fontsize{6}{7}\selectfont\sffamily, text=textsub},
    dim/.style={font=\fontsize{5.5}{6.5}\selectfont\sffamily, text=textsub},
    math/.style={font=\fontsize{7}{8}\selectfont, text=textsub},
    % Arrows
    arr/.style={-{Stealth[length=1.8mm, width=1.3mm]}, line width=0.45pt, draw=textsub},
    arrthick/.style={-{Stealth[length=2mm, width=1.5mm]}, line width=0.6pt, draw=textsub},
    arraccent/.style={-{Stealth[length=2.2mm, width=1.6mm]}, line width=0.7pt, draw=accent},
    skip/.style={-latex, line width=0.4pt, draw=textsub!50, rounded corners=2mm},
}

\begin{document}
\begin{tikzpicture}[x=1mm, y=1mm]

% ============================================
% HEADER
% ============================================
\node[font=\fontsize{14}{16}\selectfont\bfseries\sffamily, text=textmain, anchor=west]
    at (0, 72) {StageBridge Architecture};
\node[font=\fontsize{8}{9}\selectfont\sffamily, text=textsub, anchor=west]
    at (0, 66) {Receiver-centered niche encoding with OT-CFM for cell state transitions};

% ============================================
% a) TOKENIZATION + HIERARCHICAL SET TRANSFORMER
% ============================================
\begin{scope}[shift={(0, 24)}]
    \node[panel, minimum width=58mm, minimum height=56mm] at (29, -6) {};
    \node[title, anchor=west] at (4, 19) {a};
    \node[subtitle, anchor=west] at (10, 19) {Hierarchical Set Transformer};

    % 9-token niche input
    \node[annot] at (29, 13) {9-token niche sequence};

    % Token row with labels
    \node[tok=recv, minimum size=4mm] (t0) at (6, 8) {};
    \node[toklabel] at (6, 4) {recv};

    \node[tok=spatial, minimum size=4mm] (t1) at (12, 8) {};
    \node[toklabel] at (12, 4) {r1};

    \node[tok=spatial, minimum size=4mm] (t2) at (18, 8) {};
    \node[toklabel] at (18, 4) {r2};

    \node[tok=spatial, minimum size=4mm] (t3) at (24, 8) {};
    \node[toklabel] at (24, 4) {r3};

    \node[tok=spatial, minimum size=4mm] (t4) at (30, 8) {};
    \node[toklabel] at (30, 4) {r4};

    \node[tok=refcol, minimum size=4mm] (t5) at (36, 8) {};
    \node[toklabel] at (36, 4) {hlca};

    \node[tok=refcol, minimum size=4mm] (t6) at (42, 8) {};
    \node[toklabel] at (42, 4) {luca};

    \node[tok=auxcol, minimum size=4mm] (t7) at (48, 8) {};
    \node[toklabel] at (48, 4) {path};

    \node[tok=auxcol, minimum size=4mm] (t8) at (54, 8) {};
    \node[toklabel] at (54, 4) {stats};

    % ISAB 1
    \node[mhblock=encoder, minimum width=2.8cm] (isab1) at (29, -2) {ISAB$_1$};
    \node[dim, anchor=west] at (46, -2) {$m{=}8$};

    % ISAB 2 with Spatial RPE
    \node[mhblock=encoder, minimum width=2.8cm] (isab2) at (29, -10) {ISAB$_2$ + RPE};
    \node[dim, anchor=west] at (46, -10) {\textcolor{accent}{spatial}};

    % SAB
    \node[layer=attention, minimum width=2.8cm] (sab) at (29, -18) {SAB};

    % PMA
    \node[layer=drift, minimum width=2.8cm] (pma) at (29, -26) {PMA};
    \node[dim, anchor=west] at (46, -26) {$k{=}1$};

    \draw[arrthick] (29, 2) -- (isab1);
    \draw[arrthick] (isab1) -- (isab2);
    \draw[arrthick] (isab2) -- (sab);
    \draw[arrthick] (sab) -- (pma);

    % Output
    \node[iocircle=refcol, minimum size=2.2em] (ctx) at (29, -36) {$e_i$};
    \draw[arrthick] (pma) -- (ctx);

    \node[dim] at (29, -42) {context $\in \mathbb{R}^{256}$};
\end{scope}

% ============================================
% b) SPATIAL RPE DETAIL
% ============================================
\begin{scope}[shift={(64, 24)}]
    \node[panel, minimum width=56mm, minimum height=56mm] at (28, -6) {};
    \node[title, anchor=west] at (4, 19) {b};
    \node[subtitle, anchor=west] at (10, 19) {Spatial Relative Position Encoding};

    % Spatial coordinates
    \node[annot] at (28, 12) {Token coordinates $\mathbf{p}_j$};

    % Centroid
    \node[iocircle=gate, minimum size=1.6em] (cent) at (18, 4) {$\bar{\mathbf{p}}$};
    \node[dim] at (18, -1) {centroid};

    % Distance computation
    \node[layer=encoder, minimum width=2.2cm] (dist) at (40, 4) {$r_j = \|\mathbf{p}_j - \bar{\mathbf{p}}\|$};

    \draw[arr] (cent) -- (dist);

    % MLP per head
    \node[mhblock=attention, minimum width=2.6cm] (mlp) at (28, -10) {MLP$_h$};
    \node[dim, anchor=west] at (44, -10) {per head};

    \draw[arr] (dist) -- (40, -10) -- (mlp.east);

    % Bias output
    \node[layer=drift, minimum width=2.4cm] (bias) at (28, -20) {$b_j^{(h)}$};
    \draw[arr] (mlp) -- (bias);

    % Attention formula
    \node[layer=attention, minimum width=4cm] (attn) at (28, -32) {$\alpha = \text{softmax}\!\left(\frac{QK^\top}{\sqrt{d}} + b^{(h)}\right)$};
    \draw[arrthick] (bias) -- (attn);

    \node[dim] at (28, -40) {Additive bias in ISAB$_2$};
\end{scope}

% Arrow a->b
\draw[arraccent] (58, 18) -- (64, 18);

% Arrow b->c
\draw[arraccent] (120, 18) -- (126, 18);

% ============================================
% c) GROMOV-WASSERSTEIN FUSION
% ============================================
\begin{scope}[shift={(126, 24)}]
    \node[panel, minimum width=56mm, minimum height=56mm] at (28, -6) {};
    \node[title, anchor=west] at (4, 19) {c};
    \node[subtitle, anchor=west] at (10, 19) {Dual-Reference Geometry};

    % HLCA latent space (left)
    \node[layer=encoder, minimum width=1.6cm, minimum height=2.4cm] (hlca) at (14, 4) {};
    \node[dim] at (14, 12) {HLCA};
    \node[dim] at (14, -4) {$\mathbb{R}^{30}$};

    % Points in HLCA space
    \fill[encoder!80] (12, 8) circle (0.9mm);
    \fill[encoder!80] (13, 3) circle (0.9mm);
    \fill[encoder!80] (16, 6) circle (0.9mm);
    \fill[encoder!80] (15, 1) circle (0.9mm);

    % Distance annotation in HLCA
    \draw[textsub!50, line width=0.3pt] (12, 8) -- (16, 6);
    \node[dim] at (11, 5) {$d_H$};

    % LuCA latent space (right)
    \node[layer=drift, minimum width=1.6cm, minimum height=2.4cm] (luca) at (42, 4) {};
    \node[dim] at (42, 12) {LuCA};
    \node[dim] at (42, -4) {$\mathbb{R}^{10}$};

    % Points in LuCA space
    \fill[drift!80] (40, 8) circle (0.9mm);
    \fill[drift!80] (41, 3) circle (0.9mm);
    \fill[drift!80] (44, 6) circle (0.9mm);
    \fill[drift!80] (43, 1) circle (0.9mm);

    % Distance annotation in LuCA
    \draw[textsub!50, line width=0.3pt] (40, 8) -- (44, 6);
    \node[dim] at (46, 5) {$d_L$};

    % GW coupling (curved dashed lines)
    \draw[arr, draw=accent!60, dashed, bend left=20] (16, 6) to (40, 8);
    \draw[arr, draw=accent!60, dashed, bend left=10] (13, 3) to (41, 3);
    \draw[arr, draw=accent!40, dashed, bend right=10] (15, 1) to (43, 1);
    \draw[arr, draw=accent!40, dashed, bend right=20] (12, 8) to (44, 6);

    % GW optimization box - UPDATED to Pretrained
    \node[layer=attention, minimum width=2.6cm] (gw) at (28, -14) {GW Pretrained};

    \draw[arr] (hlca.south) -- (14, -14) -- (gw.west);
    \draw[arr] (luca.south) -- (42, -14) -- (gw.east);

    % Output fused embedding
    \node[iocircle=refcol, minimum size=2.4em] (fused) at (28, -26) {$z_f$};
    \draw[arrthick] (gw) -- (fused);
    \node[dim, anchor=west] at (34, -26) {$\mathbb{R}^{40}$};

    % Equation
    \node[math] at (28, -36) {Preserve $|d_H - d_L|$ structure};
    \node[dim] at (28, -42) {Neural transport map};
\end{scope}

% Arrow c->d
\draw[arraccent] (182, 22) -- (182, -8);

% ============================================
% d) CROSS-ATTENTION DRIFT
% ============================================
\begin{scope}[shift={(0, -48)}]
    \node[panel, minimum width=178mm, minimum height=54mm] at (89, 0) {};
    \node[title, anchor=west] at (4, 24) {d};
    \node[subtitle, anchor=west] at (10, 24) {CrossAttentionDrift Network};

    % Inputs
    \node[iocircle=accent] (xt) at (18, 14) {$x_t$};
    \node[iocircle=gate] (temb) at (18, 4) {$\tau$};
    \node[iocircle=refcol] (ctx) at (18, -6) {\textbf{c}};
    \node[iocircle=attention] (stg) at (18, -16) {$s$};

    \node[dim, anchor=east] at (12, 14) {state};
    \node[dim, anchor=east] at (12, 4) {time};
    \node[dim, anchor=east] at (12, -6) {context};
    \node[dim, anchor=east] at (12, -16) {stage};

    % Context path background
    \node[bgblock, minimum width=62mm, minimum height=32mm] at (68, 2) {};
    \node[annot] at (68, 18) {Context-conditioned path};

    % Query proj
    \node[layer=encoder, minimum width=2cm] (qproj) at (46, 8) {Query};
    \draw[arr] (xt.east) -- (28, 14) -- (28, 8) -- (qproj.west);
    \draw[arr] (temb.east) -- (26, 4) -- (26, 6) -- (qproj.west);

    % KV proj
    \node[layer=encoder, minimum width=2cm] (kvproj) at (46, -4) {KV};
    \draw[arr] (ctx.east) -- (kvproj.west);

    % Cross attention
    \node[mhblock=attention, minimum width=2.8cm] (mha) at (76, 2) {Cross-Attn};
    \node[dim] at (76, -5) {$H{=}4$};

    \draw[arr] (qproj.east) -- (58, 8) -- (58, 5) -- (mha.west);
    \draw[arr] (kvproj.east) -- (58, -4) -- (58, -1) -- (mha.west);
    \draw[arr] (stg.east) -- (30, -16) -- (30, -8) -- (76, -8) -- (mha.south);

    % FFN
    \node[layer=encoder, minimum width=1.8cm] (ffn) at (98, 2) {FFN};
    \draw[arr] (mha) -- (ffn);

    % v_ctx output
    \node[iocircle=refcol, minimum size=2.2em] (vctx) at (114, 2) {$v_c$};
    \draw[arr] (ffn) -- (vctx);

    % Latent path background
    \node[bgblock, minimum width=42mm, minimum height=14mm] at (76, -18) {};
    \node[annot] at (76, -11) {Latent-only path};

    % MLP
    \node[layer=encoder, minimum width=2.8cm] (mlp) at (76, -18) {MLP};
    \draw[arr] (xt.east) -- (32, 14) -- (32, -16) -- (mlp.west);
    \draw[arr] (temb.east) -- (34, 4) -- (34, -18) -- (mlp.west);
    \draw[arr] (stg.east) -- (36, -16) -- (36, -20) -- (mlp.west);

    % v_lat output
    \node[iocircle=encoder, minimum size=2.2em] (vlat) at (98, -18) {$v_\ell$};
    \draw[arr] (mlp) -- (vlat);

    % Gate
    \node[layer=gate, minimum width=2cm, minimum height=1.8cm] (gblock) at (136, -8) {$\sigma(g)$};

    \draw[arrthick] (vctx.east) -- (124, 2) -- (124, -2) -- (gblock.north);
    \draw[arrthick] (vlat.east) -- (110, -18) -- (124, -18) -- (124, -14) -- (gblock.south);

    % Output
    \node[iocircle=accent, minimum size=2.8em] (vout) at (156, -8) {$v_\theta$};
    \draw[arraccent] (gblock) -- (vout);

    % Equation
    \node[math] at (84, -28) {$v_\theta = g \cdot v_{\text{ctx}} + (1{-}g) \cdot v_{\text{lat}}$};
\end{scope}

% Context connection from GW fusion (panel c) to CrossAttentionDrift (panel d)
\draw[arrthick, draw=refcol!70] (154, -2) -- (154, -18) -| (18, -54);

% ============================================
% e) OT-CFM
% ============================================
\begin{scope}[shift={(0, -106)}]
    \node[panel, minimum width=88mm, minimum height=36mm] at (44, 0) {};
    \node[title, anchor=west] at (4, 15) {e};
    \node[subtitle, anchor=west] at (10, 15) {Optimal Transport CFM};

    % Coupling matrix
    \node[matrixbox=spatial, minimum width=18mm, minimum height=18mm] (coup) at (20, 0) {};
    \foreach \i in {1,...,3} {
        \draw[spatial!30, line width=0.3pt] ($(coup.south west) + (\i*4.5mm, 0)$) -- ($(coup.north west) + (\i*4.5mm, 0)$);
        \draw[spatial!30, line width=0.3pt] ($(coup.south west) + (0, \i*4.5mm)$) -- ($(coup.south east) + (0, \i*4.5mm)$);
    }
    \foreach \i in {1,...,3} {
        \fill[accent!50] ($(coup.south west) + (\i*4.5mm - 2.25mm, \i*4.5mm - 2.25mm)$) circle (1.2mm);
    }
    \node[annot] at (20, 0) {$\pi^*$};
    \node[dim] at (20, -12) {Sinkhorn};

    % Source/target
    \node[iocircle=spatial, minimum size=2.4em] (x0) at (46, 0) {$x_0$};
    \node[iocircle=accent, minimum size=2.4em] (x1) at (76, 0) {$x_1$};

    % Trajectory
    \draw[line width=1.6pt, accent,
        postaction={decorate, decoration={markings,
            mark=at position 0.4 with {\arrow[accent, scale=0.8]{Stealth}},
            mark=at position 0.7 with {\arrow[accent, scale=0.8]{Stealth}}}}
    ] (x0) to[out=15, in=165] (x1);

    % Intermediate
    \node[iocircle=attention, minimum size=2em] (xtmid) at (61, 4) {$x_t$};

    % Velocity
    \draw[-{Stealth[length=2.5mm, width=1.8mm]}, accent, line width=0.8pt] (xtmid) -- ++(8, 1);
    \node[dim, anchor=west] at (70, 6) {$v_\theta$};

    % Coupling arrow
    \draw[arr, draw=textsub!60] (30, 0) -- (x0);

    % Loss
    \node[math] at (52, -12) {$\mathcal{L} = \mathbb{E}_{t, \pi^*}\!\left[\|v_\theta(x_t, t \mid \mathbf{c}) - (x_1 - x_0)\|^2\right]$};
\end{scope}

% ============================================
% f) EVOLUTION BRANCH - UPDATED dimensions
% ============================================
\begin{scope}[shift={(94, -106)}]
    \node[panel, minimum width=84mm, minimum height=36mm] at (42, 0) {};
    \node[title, anchor=west] at (4, 15) {f};
    \node[subtitle, anchor=west] at (10, 15) {Evolution Branch (WES + Clonal)};

    % Input features - two boxes
    \node[iocircle=attention, minimum size=2em] (wes) at (14, 4) {WES};
    \node[dim] at (14, -2) {$\mathbb{R}^{15}$};

    \node[iocircle=encoder, minimum size=2em] (clon) at (14, -10) {Clon};
    \node[dim] at (14, -16) {$\mathbb{R}^{13}$};

    % Feature labels
    \node[annot, anchor=east] at (6, 8) {TMB};
    \node[annot, anchor=east] at (6, 4) {SBS sigs};
    \node[annot, anchor=east] at (6, 0) {OncoKB};
    \node[annot, anchor=east] at (6, -8) {CNV};
    \node[annot, anchor=east] at (6, -12) {clones};

    % Concat
    \node[layer=gate, minimum width=1.4cm] (cat) at (30, -3) {concat};
    \draw[arr] (wes) -- (22, 4) -- (22, 0) -- (cat);
    \draw[arr] (clon) -- (22, -10) -- (22, -6) -- (cat);
    \node[dim] at (30, -9) {$\mathbb{R}^{28}$};

    % Projection MLP
    \node[layer=encoder, minimum width=2cm] (proj) at (48, -3) {Proj};
    \node[dim] at (48, -9) {GELU + LN};
    \draw[arr] (cat) -- (proj);

    % Evolution embedding
    \node[iocircle=encoder, minimum size=2em] (evo) at (64, -3) {$e_\text{evo}$};
    \draw[arr] (proj) -- (evo);

    % Context input
    \node[iocircle=refcol, minimum size=2em] (ctxf) at (64, 8) {$e_i$};
    \node[dim, anchor=west] at (70, 8) {from (a)};

    % Gate mechanism
    \node[layer=gate, minimum width=1.8cm] (gatef) at (78, 2) {$\sigma(g)$};
    \draw[arr] (evo) -- (evo |- gatef) -- (gatef);
    \draw[arr] (ctxf) -- (ctxf |- gatef) -- (gatef);

    % Output
    \node[iocircle=accent, minimum size=2.2em] (outf) at (78, -10) {$\tilde{e}_i$};
    \draw[arrthick] (gatef) -- (outf);
\end{scope}

% ============================================
% g) OUTPUT HEADS - NEW PANEL
% ============================================
\begin{scope}[shift={(0, -148)}]
    \node[panel, minimum width=178mm, minimum height=28mm] at (89, 0) {};
    \node[title, anchor=west] at (4, 11) {g};
    \node[subtitle, anchor=west] at (10, 11) {Prediction Heads};

    % Context input
    \node[iocircle=refcol, minimum size=2.2em] (ctxg) at (24, 0) {$e_i$};
    \node[dim] at (24, -10) {context};

    % Stage head
    \node[layer=attention, minimum width=2.4cm] (stageh) at (60, 4) {StageHead};
    \draw[arr] (ctxg) -- (40, 0) -- (40, 4) -- (stageh);
    \node[iocircle=attention, minimum size=2em] (stageout) at (84, 4) {$\hat{s}$};
    \draw[arr] (stageh) -- (stageout);
    \node[dim, anchor=west] at (90, 4) {3 classes};

    % Pathway head
    \node[layer=drift, minimum width=2.4cm] (pathh) at (60, -4) {PathwayHead};
    \draw[arr] (ctxg) -- (40, 0) -- (40, -4) -- (pathh);
    \node[iocircle=drift, minimum size=2em] (pathout) at (84, -4) {$\hat{p}$};
    \draw[arr] (pathh) -- (pathout);
    \node[dim, anchor=west] at (90, -4) {50 pathways};

    % Proliferation head
    \node[layer=encoder, minimum width=2.4cm] (prolifh) at (130, 0) {ProlifHead};
    \draw[arr] (ctxg) -- (40, 0) -- (110, 0) -- (prolifh);
    \node[iocircle=encoder, minimum size=2em] (prolifout) at (154, 0) {$\hat{r}$};
    \draw[arr] (prolifh) -- (prolifout);
    \node[dim, anchor=west] at (160, 0) {scalar};

    % Loss annotation
    \node[math] at (89, -12) {$\mathcal{L}_{\text{aux}} = \mathcal{L}_{\text{stage}} + \lambda_p \mathcal{L}_{\text{pathway}} + \lambda_r \mathcal{L}_{\text{prolif}}$};
\end{scope}

% ============================================
% LEGEND
% ============================================
\begin{scope}[shift={(0, -182)}]
    \node[tok=recv, minimum size=5mm] at (10, 0) {};
    \node[annot, anchor=west] at (14, 0) {receiver};

    \node[tok=spatial, minimum size=5mm] at (40, 0) {};
    \node[annot, anchor=west] at (44, 0) {spatial (ring1-4)};

    \node[tok=refcol, minimum size=5mm] at (76, 0) {};
    \node[annot, anchor=west] at (80, 0) {reference (hlca/luca)};

    \node[tok=auxcol, minimum size=5mm] at (120, 0) {};
    \node[annot, anchor=west] at (124, 0) {auxiliary (pathway/stats)};

    \node[layer=gate, minimum width=1cm, minimum height=0.5cm] at (164, 0) {};
    \node[annot, anchor=west] at (170, 0) {gate};
\end{scope}

\end{tikzpicture}
\end{document}
